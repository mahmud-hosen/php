1. Все значения инициализируются в глобальной зоне видимости засоряя её.
2.  Комментарии вверху лишние
3. Не совсем ясно для чего отображение включается в верху
4. spl_autoload_register - можно в какой-то бутстрап файл вынести
5. $tableName = "table"; - имя таблицы таблица. не совсем понятно, зачем оно включается в переменную, и это в глобальной видимости.
6. слишком много логики в индексном файле. Не раскрыты знания паттернов или стиля кодирования
7. print_r($instance->fillTable($data)); - не понятна причина вывода таким способом статусов заполнения таблицы
8. //$loaderCSV->getLinks($fileName); - закомментированый код в репозитории
9. header('Content-type: application/json'); - нет буферизации вывода, а заголовок устанавливается после отправки первого байта, что должно вызвать ошибку

https://github.com/jhd235/PHP-REST-API/blob/master/classes/ConnectDb.php

1. private $host = 'localhost'; -  параметры подключения сразу в файле
 2.   //private $host = 'srv-pleskdb34.ps.kz:3306';
если это сингелтон, то не закрыты вызовы конструктора.
если рнр 7+ то есть возвращаемые типы, они не используются
3. методы statusToTinyInt и TinyIntToString делают одно и тоже, при этом  используется оператор switch, но нет обработки
4.  private function inverter($int)
    {
        switch ($int) {
            case 0:
                return 1;
            case 1:
                return 0;
        }
    }
можно просто восклицательный знак поставить
5. метод randomStatus кроме ошибок fillTable, ещё инъекции         $sqlSelect = "select status from table where id-aktsii = $rid";
6. $conn = $this->getConnection(); - не понятна цель